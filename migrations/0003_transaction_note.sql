-- 取引履歴にメモを足す。
--
-- 入出金の明細を手で打てるようにするための列（docs/spec-changes.md 3）。
-- 「何に使ったか」はここを読めば分かる。分類（カテゴリ）は持たない。
-- 持たせると集計が要り、このアプリが実質資産を見る道具から家計簿に寄る。
--
-- テーブルの作り直しではなく ALTER にしたのは、制約も列名も変えないため。
-- 0002 で loans を作り直したのは、名前付き CHECK 制約の名前を変える必要が
-- あったから。ここにはその事情が無い。
--
-- 既存行（貸し借り・ウィッシュ由来の履歴）は DEFAULT '' で空になる。
-- これらはもともと参照先を持ち、メモを書く場所ではない。
ALTER TABLE transactions ADD COLUMN note TEXT NOT NULL DEFAULT '';
